node_modules/
.venv/
.DS_Store
*.log
.env
js/config.js
js/config.dev.js
todo.txt
index.dev.html
scripts/__tests__/fixtures/temp-family-feud-test.json
data/jeopardy-parser/

# Rebuildable intermediate: `npm run ingest-llm-rounds` regenerates it from
# example games. The consumed artifact is data/llm-train/*.jsonl, which IS committed.
data/llm-rounds.db

# Question-quality A/B eval output (scorecards, blind-review sheets, answer keys).
eval-output/

# Build output (regenerated by npm run build)
public/

# Regenerable LLM disqualification cache — not a source of truth
data/disqualify-cache.json

